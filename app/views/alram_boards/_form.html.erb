<%= form_with(model: alram_board, local: true) do |form| %>
  <% if alram_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alram_board.errors.count, "error") %> prohibited this alram_board from being saved:</h2>

      <ul>
        <% alram_board.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :board_id %>
    <%= form.text_field :board_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
