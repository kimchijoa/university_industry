<p id="notice"><%= notice %></p>

<ul>
<% @my_alrams.each do |row| %>
	<li>회원님의 <%= AlramTest.find(row.board_id).title %> 게시글에 <%= link_to "#{row.post_link}" do %><%= row.new_post_title %>댓글이 등록되었습니다.<% end %></li>
<% end %>	
</ul>


<h1>Alram Tests</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Desc</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @alram_tests.each do |alram_test| %>
      <tr>
        <td><%= alram_test.title %></td>
        <td><%= alram_test.desc %></td>
        <td><%= alram_test.user_id %></td>
        <td><%= link_to 'Show', alram_test %></td>
        <td><%= link_to 'Edit', edit_alram_test_path(alram_test) %></td>
        <td><%= link_to 'Destroy', alram_test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Alram Test', new_alram_test_path %>
